"use client"

const Loading = () => {
  return (
    <div className="w-full h-screen flex items-center justify-center">
      <div className="w-10 h-10 rounded-full bg-blue-600 animate-ping animate-bounce transition-all duration-500" />
    </div>
  )
}

export default Loading
